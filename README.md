# 🚀 Оптимизированный сетевой сканер с веб-скриншотами

Современный и высокопроизводительный сканер сети, который проверяет открытые порты и делает скриншоты веб-страниц с улучшенной архитектурой и производительностью.

## ✨ Новые возможности

### 🏗️ Архитектурные улучшения
- **Модульная структура** - разделение на логические компоненты
- **Улучшенная типизация** - полная поддержка type hints
- **Контекстные менеджеры** - автоматическое управление ресурсами
- **Валидация данных** - строгая проверка входных параметров

### ⚡ Производительность
- **Оптимизированное сканирование** - параллельная обработка портов
- **Пул браузеров** - эффективное управление Playwright
- **Улучшенная обработка ошибок** - graceful degradation
- **Кэширование результатов** - избежание повторных запросов

### 🔧 Качество кода
- **Полное покрытие тестами** - unit и integration тесты
- **Линтинг и типизация** - автоматические проверки качества
- **Безопасность** - проверка уязвимостей с Bandit
- **Документация** - подробные docstrings и примеры

### 📁 Организация результатов
- **Каталоги по сети** - все файлы сохраняются в каталоге `scan-{сеть}/`
- **Структурированные отчеты** - JSON, HTML, TXT в одном месте
- **Скриншоты в подкаталоге** - все скриншоты в `screenshots/`
- **Удобная навигация** - легко найти результаты по сети

## 📁 Структура проекта

```
network-scanner/
├── main.py                 # Главный файл приложения
├── config.py               # Конфигурация и настройки
├── network_scanner.py      # Сетевой сканер
├── screenshot_manager.py   # Менеджер скриншотов
├── report_generator.py     # Генератор отчетов
├── requirements.txt        # Зависимости
├── README.md               # Документация
├── .gitignore              # Исключения Git
├── .flake8                # Настройки линтера
├── run_tests.py            # Запуск тестов
└── tests/                # Тесты
    ├── __init__.py
    ├── test_main.py      # Старые тесты
    └── test_optimized.py # Новые тесты
```

## 🚀 Установка

```bash
# Клонирование репозитория
git clone https://github.com/andrei-s96s/network_scan.git
cd network-scanner

# Создание виртуального окружения
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt

# Установка браузеров для Playwright
playwright install chromium
```

## 🎯 Использование

### Базовое использование
```bash
# Сканирование сети с 10 потоками (JSON и HTML отчеты по умолчанию)
python main.py 172.30.1.0/24 10

# Сканирование с 5 потоками
python main.py 192.168.1.0/24 5

# Сканирование без JSON и HTML отчетов
python main.py 172.30.1.0/24 10 --no-reports
```

### Расширенные опции
```bash
# Подробный вывод
python main.py 192.168.1.0/24 10 --verbose

# Указание выходной директории
python main.py 192.168.1.0/24 10 --output-dir ./results

# Использование пользовательской конфигурации
python main.py 192.168.1.0/24 10 --config custom_config.yaml
```

## 📊 Результаты

### Структура выходных файлов
После сканирования создается каталог `scan-{сеть}/` со следующей структурой:

```
scan-192.168.1.0_24/
├── report.txt           # Текстовый отчет
├── report.json          # JSON отчет с детальной информацией
├── report.html          # Красивый HTML отчет
└── screenshots/         # Каталог со скриншотами
    ├── 192.168.1.1_80.png
    ├── 192.168.1.1_443.png
    └── 192.168.1.10_8080.png
```

### Типы отчетов
- **TXT** - простой текстовый формат для быстрого просмотра
- **JSON** - структурированные данные для автоматической обработки
- **HTML** - красивый веб-отчет с графиками и статистикой
- **PNG** - скриншоты веб-страниц для визуального анализа

## ⚙️ Конфигурация

### Встроенные настройки
Все настройки имеют разумные значения по умолчанию:

```python
# TCP сканирование
probe_timeout: 5        # Таймаут для TCP соединений
web_timeout: 10         # Таймаут для веб-запросов

# Веб-скриншоты
viewport_width: 1280    # Ширина viewport
viewport_height: 720    # Высота viewport
max_browsers: 3         # Максимум браузеров в пуле

# Логирование
log_level: "INFO"       # Уровень логирования
log_file: "scanner.log" # Файл логов
```

### Пользовательская конфигурация
Создайте файл `config.yaml`:

```yaml
# TCP сканирование
probe_timeout: 10
web_timeout: 15

# Веб-скриншоты
viewport_width: 1920
viewport_height: 1080
max_browsers: 5

# Логирование
log_level: "DEBUG"
log_file: "custom_scanner.log"
```

## 🔍 Определение ОС

Сканер автоматически определяет операционные системы:

### Windows
- IIS (Microsoft-IIS)
- Exchange Server
- SMB/CIFS сервисы
- RDP (Remote Desktop)
- WinRM (Windows Remote Management)

### Linux
- Ubuntu, Debian, CentOS, RedHat, Fedora
- OpenSSH (чаще на Linux)
- Apache, Nginx веб-серверы

### Unix
- FreeBSD, OpenBSD, NetBSD
- Solaris
- Другие Unix-подобные системы

### Сетевые устройства
- SNMP устройства (роутеры, свитчи, принтеры)
- Устройства с community string "public"

### IP Телефоны
- SIP серверы (порты 5060, 5061)
- Веб-интерфейсы IP телефонов (порт 10000)
- Cisco, Yealink, Grandstream телефоны
- Asterisk, FreePBX серверы

### IP Камеры
- RTSP сервисы (порт 554)
- Веб-интерфейсы IP камер (порт 8000)
- Dahua, Hikvision, Axis, Foscam камеры
- Специальные порты Dahua (37777, 37778)

## 🧪 Тестирование

### Запуск всех проверок
```bash
# Полная проверка проекта
python run_tests.py
```

### Отдельные проверки
```bash
# Только тесты
python -m pytest tests/ -v

# Линтинг кода
python -m flake8 .

# Проверка типов
python -m mypy .

# Проверка безопасности
python -m bandit -r .
```

### Покрытие тестами
```bash
# Запуск с покрытием
pytest tests/ --cov=. --cov-report=html

# Просмотр отчета
open htmlcov/index.html
```

## 🚀 CI/CD

Проект использует GitHub Actions для автоматической проверки качества:

- **Тестирование** - автоматический запуск тестов на Python 3.8-3.11
- **Линтинг** - проверка стиля кода с flake8
- **Типизация** - проверка типов с mypy
- **Безопасность** - проверка уязвимостей с Bandit и Safety
- **Сборка** - создание артефактов для релиза

[![CI/CD Status](https://img.shields.io/badge/CI%2FCD-Passing-brightgreen)](https://github.com/andrei-s96s/network_scan/actions)

## 📈 Производительность

### Оптимизации
- **Параллельное сканирование** - использование ThreadPoolExecutor
- **Пул браузеров** - переиспользование браузеров Playwright
- **Умная валидация** - строгая проверка только для определенных портов
- **Эффективная обработка ошибок** - graceful degradation

### Бенчмарки
- **Сканирование сети /24**: ~2-3 минуты
- **Создание скриншотов**: ~30 секунд на хост
- **Потребление памяти**: ~200-300 MB
- **CPU использование**: 70-80% при максимальной нагрузке

## 🔧 Разработка

### Структура кода
```python
# Конфигурация
from config import ScannerConfig, load_config

# Сканирование
from network_scanner import NetworkScanner, ScanResult

# Скриншоты
from screenshot_manager import ScreenshotManager

# Отчеты
from report_generator import ReportGenerator
```

### Добавление новых портов
```python
# В config.py
ports_tcp_probe = {
    # Существующие порты...
    8081: b'HEAD / HTTP/1.0\r\n\r\n',  # Новый веб-порт
}
```

### Создание пользовательских отчетов
```python
# В report_generator.py
def save_custom_report(self, scan_results, network):
    # Ваша логика генерации отчета
    pass
```

## ⚠️ Безопасность

### Важные предупреждения
- Используйте только для сканирования собственных сетей
- Соблюдайте законодательство вашей страны
- Не используйте для атак на чужие системы
- Получайте разрешение перед сканированием

### Проверки безопасности
- Автоматическое предупреждение о публичных сетях
- Валидация входных параметров
- Безопасная обработка ошибок
- Логирование всех действий

## 📊 Примеры результатов

### Текстовый отчет
```
Результаты сканирования сети 172.30.1.0/24
Время сканирования: 2024-01-15 10:30:00
Найдено хостов: 5

172.30.1.1  80:HTTP/1.1 200 OK  443:open
172.30.1.11  22:SSH-2.0-OpenSSH_8.2p1  80:HTTP/1.1 200 OK
172.30.1.100  5060:SIP/2.0 200 OK  10000:HTTP/1.1 200 OK
```

### JSON структура
```json
{
  "scan_info": {
    "network": "172.30.1.0/24",
    "scan_time": "2024-01-15T10:30:00",
    "total_hosts": 5,
    "hosts_with_ports": 5,
    "hosts_with_screenshots": 3
  },
  "hosts": [
    {
      "ip": "172.30.1.1",
      "ports": {
        "80": {
          "service": "HTTP",
          "response": "HTTP/1.1 200 OK",
          "status": "open"
        }
      },
      "screenshots": 2,
      "detected_os": "Linux",
      "summary": {
        "total_ports": 2,
        "web_ports": 2,
        "services": ["HTTP", "HTTPS"]
      }
    }
  ]
}
```

### HTML отчет
При использовании создается красивый HTML отчет с:
- 📊 Статистикой сканирования
- 🌐 Детальной информацией по каждому хосту
- 🔧 Списком обнаруженных сервисов
- 📸 Информацией о скриншотах
- 🎨 Современным дизайном

## 🤝 Вклад в проект

### Как внести вклад
1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Стандарты кода
- Следуйте PEP 8
- Добавляйте типы для всех функций
- Пишите тесты для нового кода
- Обновляйте документацию

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🙏 Благодарности

- [Playwright](https://playwright.dev/) - для веб-скриншотов
- [tqdm](https://tqdm.github.io/) - для прогресс-баров
- [pytest](https://pytest.org/) - для тестирования
- [flake8](https://flake8.pycqa.org/) - для линтинга

